# Core dependencies
typing-extensions>=4.5.0
python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.8.0
wget>=3.2  # For downloading models
ollama>=0.1.0  # Add this for Ollama support

# LLM dependency
ctransformers>=0.2.24  # Lightweight LLM implementation for ARM

# LLM options - choose one
gpt4all==0.1.7  # Older version compatible with Raspberry Pi
# ctransformers[cuda]  # Lightweight alternative for LLMs
llama-cpp-python>=0.2.0

# Security
cryptography>=42.0.0 